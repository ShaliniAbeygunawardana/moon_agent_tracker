apiVersion: v1
kind: Service
metadata:
  name: aggregation-service
spec:
  selector:
    app: aggregation-service
  type: NodePort
  ports:
    - protocol: TCP
      port: 8002
      targetPort: 8002
      nodePort: 30082